name: Deploy to Github Pages
on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 15
      # - name: Setup chrome
      #   uses: ./.github/actions/setup-chrome
      # - run: docker run -d -p 4444:4444 -v /dev/shm:/dev/shm selenium/standalone-chrome:4.0.0-rc-1-prerelease-20210618
      - run: npm ci
      # - run: npm run build --if-present
      - run: npm test unit
      - run: xvfb-run npm run e2e
        env: 
          DISPLAY: 99.0
    # gh-pages-deploy:
    #   name: Deploying to Github Pages
    #   runs-on: ubuntu-latest
    #   steps:
    #       - uses: actions/checkout@v2
    #       - uses: actions/setup-node@v2
    #         with:
    #           node-version: '12'
    #       - name: Install packages
    #         run: npm i
    #       - name: Set Creds
    #         run: git config user.name "<username>" && git config user.email "<email>"
    #       - name: Deploy
    #         run: npm run deploy